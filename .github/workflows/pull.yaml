name: Run on Any Push

on:
  push:       # triggers on any branch

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "This workflow runs on any branch push!"
