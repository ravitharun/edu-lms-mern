const ApplyLeave = async (req, res) => {
    // const { ApplyLeave } = req.body
    // return res.json({message:"do"})
    // if (!ApplyLeave) return res.status(404).json({ message: "Something Went Wrong." })

    
}

module.exports={ApplyLeave}